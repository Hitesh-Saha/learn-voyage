export type Categories = {
    id: number;
    name: string;
    icon: string;
}

export type Tab = 'home' | 'search' | 'mycourses' | 'saved' | 'profile';

export type Mode = "login" | "signup" | "welcome";